/*
 * Electrical Career Readiness Hub — Learning Engine v2 adapter
 * Canonical runtime helper for the Learn → Apply → Check → Evidence loop.
 * Framework-free so the existing index.html can adopt it without replacing
 * the current production shell.
 */
export const STAGES = ['learn', 'apply', 'check', 'evidence'];
export const STAGE_LABELS = { learn: 'Learn', apply: 'Apply', check: 'Check', evidence: 'Evidence' };
export const CURRICULUM_URLS = {
  1: '/curriculum/learning-content-v1.json', 2: '/curriculum/week-02-microstation-connect.json', 3: '/curriculum/week-03-electrical-documentation.json', 4: '/curriculum/week-04-single-line-diagrams.json', 5: '/curriculum/week-05-cable-schedules.json', 6: '/curriculum/week-06-asnzs-3000-application.json', 7: '/curriculum/week-07-drawing-package-ownership.json', 8: '/curriculum/week-08-pdb-layouts.json', 9: '/curriculum/week-09-cable-sizing-voltage-drop.json', 10: '/curriculum/week-10-protection-discrimination.json'
};
export function emptyProgress() { return { learn: false, apply: false, check: false, evidence: false }; }
export function normalizeWeek(content, fallback = {}) { const c = content || {}, integration = c.integration || {}; return { id: c.id || fallback.id, week: c.week ?? fallback.week, title: c.title || fallback.title || 'Untitled week', phase: c.phase || fallback.phase || '', estimatedHours: c.estimatedHours ?? fallback.estimatedHours ?? 6, objective: c.objective || c.learn?.objective || '', learn: c.learn || { heading: 'Core learning', bullets: [], takeaway: '' }, apply: c.apply || { scenario: '', tasks: [], deliverable: '' }, check: c.check || { questions: [], passRule: '' }, evidence: c.evidence || { prompt: '', criteria: [], portfolioCategory: '' }, skills: c.skills || c.skillTargets || integration.skills || [], integration: { ...integration, homeAction: integration.homeAction || integration.home || '', journalPrompt: integration.journalPrompt || integration.journal || '', portfolioPrompt: integration.portfolioPrompt || integration.portfolio || '' } }; }
export function stageProgress(progress) { const p = { ...emptyProgress(), ...(progress || {}) }; return STAGES.reduce((n, stage) => n + (p[stage] ? 1 : 0), 0); }
export function totalProgress(progressByWeek) { return Object.values(progressByWeek || {}).reduce((n, p) => n + stageProgress(p), 0); }
export function nextStage(progressByWeek, weekIds) { for (const id of weekIds || Object.keys(progressByWeek || {})) { const p = progressByWeek?.[id] || emptyProgress(); for (const stage of STAGES) if (!p[stage]) return { weekId: id, stage }; } return null; }
export function isStageUnlocked(progressByWeek, weekId, stage) { const p = progressByWeek?.[weekId] || emptyProgress(), index = STAGES.indexOf(stage); if (index < 0) return false; if (index === 0) return true; return STAGES.slice(0, index).every(previous => Boolean(p[previous])); }
export function canCompleteStage(stage, context = {}) {
  if (stage === 'learn') return Boolean(context.learnViewedAt);
  if (stage === 'apply') return Boolean(context.applicationNotes?.trim());
  if (stage === 'check') {
    if (context.assessmentResult) return Boolean(context.assessmentResult.completionReady && context.assessmentResult.passed);
    return Number.isFinite(context.score) && context.total > 0 && context.score >= Math.ceil(context.total * 0.67);
  }
  if (stage === 'evidence') return Boolean(context.evidence?.title?.trim() && context.evidence?.description?.trim());
  return false;
}
export function deriveSignals(week, progress, context = {}) { const p = { ...emptyProgress(), ...(progress || {}) }; return { completedStages: STAGES.filter(s => p[s]), completion: stageProgress(p), evidenceReady: Boolean(context.evidence?.title?.trim() && context.evidence?.description?.trim()), skills: week?.skills || [], journalPrompt: week?.integration?.journalPrompt || '', homeAction: week?.integration?.homeAction || '', portfolioPrompt: week?.integration?.portfolioPrompt || '' }; }
export function createLearningState(weekIds = Object.keys(CURRICULUM_URLS)) { return weekIds.reduce((state, id) => { state[id] = emptyProgress(); return state; }, {}); }
export function migrateLegacyProgress(legacyWeeks, weekIds = Object.keys(CURRICULUM_URLS)) { const ids = weekIds.map(String), source = Array.isArray(legacyWeeks) ? legacyWeeks : [], canonical = createLearningState(ids); ids.forEach((id, index) => { canonical[id] = { ...emptyProgress(), ...(source[index] || {}) }; }); return canonical; }
export function mergeLearningProgress(current, incoming, weekIds = Object.keys(CURRICULUM_URLS)) { const ids = weekIds.map(String), base = createLearningState(ids); const a = Array.isArray(current) ? migrateLegacyProgress(current, ids) : (current || {}), b = Array.isArray(incoming) ? migrateLegacyProgress(incoming, ids) : (incoming || {}); ids.forEach(id => { base[id] = { ...emptyProgress(), ...(a[id] || {}), ...(b[id] || {}) }; }); return base; }
export function applyStageCompletion(progressByWeek, weekId, stage, context = {}) { if (!STAGES.includes(stage)) throw new Error(`Unknown learning stage: ${stage}`); const current = progressByWeek?.[weekId] || emptyProgress(); if (!isStageUnlocked(progressByWeek, weekId, stage)) return { ok: false, progress: { ...current }, reason: 'Stage locked: complete the previous stage first' }; if (!canCompleteStage(stage, context)) return { ok: false, progress: { ...current }, reason: `Stage gate not satisfied: ${STAGE_LABELS[stage]}` }; return { ok: true, progress: { ...current, [stage]: true }, reason: `${STAGE_LABELS[stage]} completed` }; }
export function commitStageCompletion({ catalog = {}, progressByWeek = {}, weekId, stage, context = {}, contextByWeek = {}, journalEntries = [], portfolioEntries = [] } = {}) { const ids = Object.keys(catalog || {}).sort((a, b) => Number(a) - Number(b)); const current = mergeLearningProgress(progressByWeek, {}, ids.length ? ids : Object.keys(progressByWeek || {})); const result = applyStageCompletion(current, String(weekId), stage, context); if (!result.ok) return { ...result, progressByWeek: current, hubSignals: buildHubSignals(catalog, current, contextByWeek, journalEntries, portfolioEntries) }; const nextProgress = { ...current, [String(weekId)]: result.progress }; const nextContext = { ...contextByWeek, [String(weekId)]: { ...(contextByWeek?.[String(weekId)] || {}), ...context } }; return { ...result, progressByWeek: nextProgress, contextByWeek: nextContext, hubSignals: buildHubSignals(catalog, nextProgress, nextContext, journalEntries, portfolioEntries) }; }
export async function loadWeek(url, fallback = {}) { const response = await fetch(url, { cache: 'no-store' }); if (!response.ok) throw new Error(`Curriculum load failed: ${response.status} ${url}`); return normalizeWeek(await response.json(), fallback); }
export async function loadCatalog(urls = CURRICULUM_URLS) { if (urls === CURRICULUM_URLS) { const { loadCanonicalCatalog } = await import('./canonical-catalog-v1.js'); return loadCanonicalCatalog(); } const entries = await Promise.all(Object.entries(urls).map(async ([week, url]) => [String(week), await loadWeek(url, { week: Number(week) })])); return Object.fromEntries(entries); }
export function buildIntegrationSnapshot(catalog, progressByWeek, contextByWeek = {}) { const weeks = Object.entries(catalog || {}).map(([weekId, week]) => { const progress = progressByWeek?.[weekId] || emptyProgress(); return { weekId, title: week.title, progress, signals: deriveSignals(week, progress, contextByWeek?.[weekId] || {}) }; }); const next = nextStage(progressByWeek, Object.keys(catalog || {})); return { next, totalStages: Object.keys(catalog || {}).length * STAGES.length, completedStages: weeks.reduce((n, w) => n + w.signals.completion, 0), weeks }; }
export function buildHubSignals(catalog, progressByWeek = {}, contextByWeek = {}, journalEntries = [], portfolioEntries = []) { const weekIds = Object.keys(catalog || {}).sort((a, b) => Number(a) - Number(b)); const totalStages = weekIds.length * STAGES.length; const completedStages = totalProgress(progressByWeek); const evidenceWeeks = weekIds.filter(id => Boolean(progressByWeek?.[id]?.evidence)); const next = nextStage(progressByWeek, weekIds); const skillMap = {}; const prioritySkillGaps = []; for (const id of weekIds) { const week = catalog[id]; const progress = progressByWeek?.[id] || emptyProgress(); for (const skill of week?.skills || []) { const key = String(skill); const item = skillMap[key] || { skill: key, weeks: 0, completedWeeks: 0, demonstratedWeeks: 0, knowledgeChecks: 0, evidenceQuality: 0 }; item.weeks += 1; if (progress.evidence) item.demonstratedWeeks += 1; if (progress.check) item.knowledgeChecks += 1; if (progress.evidence) item.evidenceQuality += 1; if (stageProgress(progress) === STAGES.length) item.completedWeeks += 1; skillMap[key] = item; } } Object.values(skillMap).forEach(item => { const readiness = item.weeks ? Math.round(((item.completedWeeks + item.demonstratedWeeks) / (item.weeks * 2)) * 100) : 0; item.readiness = Math.min(100, readiness); if (item.readiness < 60) prioritySkillGaps.push({ skill: item.skill, readiness: item.readiness, reason: 'Needs more demonstrated capability and/or completed learning stages' }); }); prioritySkillGaps.sort((a, b) => a.readiness - b.readiness); const studyHours = (journalEntries || []).reduce((sum, entry) => sum + (Number(entry?.hours) || 0), 0); const reflections = (journalEntries || []).filter(entry => Boolean(String(entry?.reflection || entry?.learn || entry?.whatLearned || '').trim())).length; const nextActions = (journalEntries || []).map(entry => String(entry?.nextAction || entry?.next || '').trim()).filter(Boolean); const evidenceWeeksCount = evidenceWeeks.length; const evidenceRecordCount = new Set([...evidenceWeeks.map(id => `week:${id}`), ...(portfolioEntries || []).map((entry, index) => `entry:${entry?.week ?? index}:${entry?.title ?? index}`)]).size; const knowledgeChecksPassed = weekIds.filter(id => Boolean(progressByWeek?.[id]?.check)).length; const evidenceCompletionRate = weekIds.length ? Math.round((evidenceWeeksCount / weekIds.length) * 100) : 0; const nextWeek = next ? catalog[next.weekId] : null; const demonstratedCapability = Object.values(skillMap).map(item => ({ skill: item.skill, score: Math.min(5, Math.round(item.readiness / 20)), target: 5, readiness: item.readiness, evidenceCount: item.demonstratedWeeks, knowledgeChecks: item.knowledgeChecks })).sort((a, b) => b.readiness - a.readiness); return { overallProgress: totalStages ? Math.round((completedStages / totalStages) * 100) : 0, completedStages, totalStages, nextBestAction: next ? { weekId: next.weekId, week: nextWeek?.title || `Week ${next.weekId}`, stage: next.stage, label: STAGE_LABELS[next.stage], prompt: nextWeek?.integration?.homeAction || '' } : null, studyMomentum: { studyHours, reflectionCount: reflections, portfolioCount: evidenceRecordCount, journalEntryCount: (journalEntries || []).length }, prioritySkillGaps: prioritySkillGaps.slice(0, 8), skills: Object.values(skillMap).sort((a, b) => b.readiness - a.readiness), demonstratedCapability, journal: { studyHours, hoursThisWeek: studyHours, reflectionCount: reflections, entryCount: (journalEntries || []).length, nextAction: nextActions[0] || '', entries: journalEntries || [] }, portfolio: { evidenceCount: evidenceRecordCount, evidenceCompletionRate: evidenceCompletionRate / 100, evidenceWeeks, entries: portfolioEntries || [] }, evidence: { evidenceReadyWeeks: evidenceWeeks.length, evidenceRate: evidenceCompletionRate }, knowledgeChecksPassed, generatedAt: new Date().toISOString() }; }
